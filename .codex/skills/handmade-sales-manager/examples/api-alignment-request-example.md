# 依頼例: API整合確認

このスキルを使って、`GET /api/products` のレスポンスが API仕様書と一致しているか確認し、差分があれば最小差分で修正してください。

## 条件
- `thumbnailUrl` の扱い
- `meta.page`, `meta.pageSize`, `meta.totalCount`, `meta.hasNext`
- 既存 UI 影響も確認
